This environment was set up as follows:

```
python3 -m venv venv
source venv/bin/activate
pip install pandas numpy
```

Dependencies were recorded with this command:

```
pip freeze > requirements.txt
```

To replicate this environment, make a venv and install dependencies with the following command:

```
pip install -r requirements.txt
```
